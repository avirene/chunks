<h1>Your Entries</h1>

<%= link_to "New entry", new_entry_path %>

<table>
  <tr>
    <th>Word</th>
    <th>Definition</>
    <th>Example</>
    <th></th>
  </tr>

  <% @entries.each do |entry| %>
    <tr>
      <td><%= entry.word %></td>
      <td><%= entry.definition %></td>
      <td><%= entry.example %></td>
      <td><%= link_to "Show", entry_path(entry) %></td>
      <td><%= link_to "Edit", edit_entry_path(entry) %></td>
      <td><%= link_to "Destroy", entry_path(entry), method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
  <% end %>
</table>

<button onclick="practiceWords()">Practice words</button>
<button onclick="practiceChunks()">Practice chunks</button>

<script type="text/javascript" src="src/word.js"></script>
<script type="text/javascript" src="src/chunk.js"></script>
